
<article class="card">
  <header>
    <h2>Tarefas</h2>
  </header>
  <% if person.responsible.nil? %>
    <div class="alert alert-warning">
      Não pronto para receber tarefas
      <%= turbo_frame_tag "create_responsible" do %>
        <%= button_to(
          responsibles_path(person_id: person.id),
          method: :post,
          class: "btn btn-primary",
          data: { turbo_frame: "create_responsible" }
        ) do %>
        Tornar responsável
      <% end %>
    <% end %>
    </div>
  <% else %>
    <div class="task-summary">
      <turbo_frame id="my-tasks" src="/tasks/summary/?responsible_id=<%= person.responsible.id %>">
        This is should have been replaced
      </turbo_frame>

      <%= turbo_frame_tag "task_summary" do %>
        <%= link_to(
          "Ver tarefas",
          tasks_path(responsible_id: person.responsible.id),
          class: "btn btn-secondary mt-2",
          data: { turbo_frame: "_top" }
        ) %>
    <% end %>
    </div>
  <% end %>
</article>
