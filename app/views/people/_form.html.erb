
<%= form_with model: @person do |f| %>
  <div class="field">
    <%= f.label :name, 'Nome'  %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :borned_on, 'Data de Nascimento'  %>
    <%= f.date_field :birth %>
  </div>
  <div class="field">
    <%= f.label :dead_on, 'Data de Nascimento'  %>
    <%= f.date_field :birth %>
  </div>
  <fieldset>
    <legend>Nacionalidades</legend>
    <%= f.fields_for :nationalities do |ff| %>
      <div class="field">
        <%= ff.hidden_field :person_id, value: @person.id %>
        <%= ff.label :country, 'País'  %>
        <%= ff.select :country_id, options_for_select(
          @countries.map { |c| [c.name, c.id]}
        ) %>
      </div>
      <div class="field">
        <% @nationalitiy_hows.map do |n| %> 
          <%= ff.label :how, n[1]  %>
          <%= ff.radio_button :how, n[0] %>
        <% end %>
      </div>
    <% end %>
  </fieldset>
  <div class="action"><%= f.submit "Salvar pessoa" %></div>
<% end %>
