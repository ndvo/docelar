<%= form_with(model: tagged) do |form| %>
  <% if tagged.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tagged.errors.count, "error") %> prohibited this tagged from being saved:</h2>

      <ul>
        <% tagged.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
